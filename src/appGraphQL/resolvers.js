import resolversQuery from './resolversQuery';
// import resolversMutation from './resolversMutation';

const resolvers = {
  Query: resolversQuery
  // Mutation: resolversMutation
};

export default resolvers;
